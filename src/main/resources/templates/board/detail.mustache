<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>게시글 상세보기</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h2 class="mb-0">{{board.boardTitle}}</h2>
        </div>
        <div class="card-body">
            <div class="mb-2">
                <strong>작성자:</strong> {{board.member.username}} (ID: {{board.member.memberIdx}})
            </div>
            <div class="mb-2"><strong>작성일:</strong> {{formattedCreatedAt}}</div>
            <div class="mb-2"><strong>수정일:</strong> {{formattedUpdatedAt}}</div>
            <div class="mb-3"><strong>조회수:</strong> {{board.boardHits}}</div>

            <hr>

            <div class="mb-4">
                <p>{{board.boardContent}}</p>
            </div>

            {{#isAuthor}}
                <form method="get" action="/board/{{board.boardIdx}}/edit" style="display:inline;">
                    <button type="submit" class="btn btn-outline-primary">수정</button>
                </form>
                <form method="post" action="/board/{{board.boardIdx}}/delete" style="display:inline;">
                    <button type="submit" class="btn btn-outline-danger">삭제</button>
                </form>
            {{/isAuthor}}

            <a href="/board/list" class="btn btn-secondary">목록으로</a>
        </div>
    </div>
</div>
</body>
</html>
