<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>{{#isEdit}}✏️ 글 수정하기{{/isEdit}}{{^isEdit}}📝 글 작성하기{{/isEdit}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
<div class="container mt-5">
    <h2 class="mb-4 text-primary">
        {{#isEdit}}✏️ 글 수정하기{{/isEdit}}
        {{^isEdit}}📝 글 작성하기{{/isEdit}}
    </h2>

    {{#isEdit}}
    <form action="/board/{{board.boardIdx}}/edit" method="post">
    {{/isEdit}}
    {{^isEdit}}
    <form action="/board" method="post">
    {{/isEdit}}

    <div class="mb-3">
        <label for="boardTitle" class="form-label">제목</label>
        <input type="text" class="form-control" id="boardTitle" name="boardTitle"
               placeholder="제목을 입력하세요"
               value="{{#isEdit}}{{board.boardTitle}}{{/isEdit}}" required />
    </div>

    <div class="mb-3">
        <label for="boardContent" class="form-label">내용</label>
        <textarea class="form-control" id="boardContent" name="boardContent" rows="8" placeholder="내용을 입력하세요" required>{{#isEdit}}{{board.boardContent}}{{/isEdit}}</textarea>
    </div>

    <div class="mb-3">
        <label for="tags" class="form-label">태그 (쉼표로 구분)</label>
        <input type="text" class="form-control" id="tags" name="tags" placeholder="예: spring, jpa, backend"
               value="{{#isEdit}}{{board.tags}}{{/isEdit}}" />
    </div>

    <button type="submit" class="btn btn-primary">
        {{#isEdit}}수정 완료{{/isEdit}}
        {{^isEdit}}등록{{/isEdit}}
    </button>
    <a href="/board/list" class="btn btn-secondary">취소</a>
</form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
